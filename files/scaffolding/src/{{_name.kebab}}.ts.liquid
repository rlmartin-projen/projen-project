import * as path from 'path';
import { cdk } from 'projen';
import { addFiles, loadSettings } from '@rlmartin-projen/projen-project';

export interface {{_name.pascal}}Options extends cdk.JsiiProjectOptions {
}

export class {{_name.pascal}} extends cdk.JsiiProject {
  constructor(options: {{_name.pascal}}Options) {
    const { options: projectOpts, files } = loadSettings(options, path.join(__dirname, '../files'), true)
    super(projectOpts);
    addFiles(this, files);
    // Add non-templated resources here
  }
}
